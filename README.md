# Веб-приложение «Рулетка с турнирной таблицей»

Учебный проект для демонстрации работы в команде.  
Веб-приложение реализовано на **Python** с использованием **SQLite** для хранения данных.  
Игрок крутит рулетку из 3 цилиндров, получает очки за комбинации, формируется турнирная таблица с ТОП-10 игроков.  

---

## 🚀 Установка и запуск

### 📋 Требования
- Python 3.8+
- pip (менеджер пакетов Python)
- PowerShell или Command Prompt (для Windows)

### 1️⃣ Клонирование репозитория

```bash
git clone https://github.com/Belandiy/roulette-game.git
cd roulette-game
```

### 2️⃣ Создание виртуального окружения (опционально)

```powershell
# Создать виртуальное окружение
python -m venv .venv

# Активировать его (Windows)
.\.venv\Scripts\Activate.ps1

# Или (Linux/macOS)
source .venv/bin/activate
```

### 3️⃣ Установка зависимостей

```bash
pip install -r requirements.txt
```

Будут установлены:
- Flask (веб-фреймворк)
- click (для CLI команд)

### 4️⃣ Инициализация базы данных

**Вариант 1: Используя Flask CLI (рекомендуется)**
```powershell
# Установить переменную окружения
$env:FLASK_APP = 'app.py'

# Инициализировать БД
flask init-db
```

**Вариант 2: Используя Python скрипт с тестовыми данными**
```bash
python init_test_db.py
```

Этот вариант создаст БД и заполнит её:
- 5 тестовых пользователей
- 13 результатов игр (для демонстрации лидерборда)

### 5️⃣ Запуск приложения

```bash
python app.py
```

**Результат:** Приложение запустится на `http://127.0.0.1:5000`

```
 * Serving Flask app 'app'
 * Debug mode: on
 * Running on http://127.0.0.1:5000
 * WARNING: This is a development server...
```

### 6️⃣ Открыть в браузере

- **Главная страница:** http://127.0.0.1:5000/
- **Страница правил:** http://127.0.0.1:5000/rules
- **Лидерборд (API):** http://127.0.0.1:5000/api/leaderboard

---

### 🧪 Проверка базы данных

**Просмотр содержимого БД:**
```bash
python check_db.py
```

Выведет:
- Количество пользователей и результатов
- Лидерборд (ТОП-10 игроков)
- Статистика БД (индексы, режим WAL, Foreign Keys)

---

### 🛑 Остановка приложения

Нажмите `Ctrl+C` в терминале, где запущено приложение.

---

### 🔄 Пересоздание базы данных

Если нужно очистить все данные и создать новую БД:

```powershell
# 1. Удалить старые файлы БД
Remove-Item database.db -Force -ErrorAction SilentlyContinue
Remove-Item database.db-wal -Force -ErrorAction SilentlyContinue
Remove-Item database.db-shm -Force -ErrorAction SilentlyContinue

# 2. Создать новую (пустую) БД
$env:FLASK_APP = 'app.py'
flask init-db

# Или с тестовыми данными
python init_test_db.py
```

---

API — POST /api/spin

Описание. Сервер честно генерирует «вращение» — 3 числа (0–9) и возвращает результат и начисленные очки. MVP: ничего не сохраняется (нет БД).
```
Endpoint: POST /api/spin
```
```
Content-Type: application/json
```
```
Request
{
  "nickname": "Player1"
}
```

nickname — необязательное. Если не передано, будет anonymous.
```bash
Response — 200 OK
{
  "nickname": "Player1",
  "result": [4, 4, 4],
  "score": 100,
  "combo": "three_of_kind"
}
```

Пояснения

result — массив из 3 чисел (0..9).

combo — одна из строк: "three_of_kind", "pair", "none".

score — начисленные очки (MVP-правила: три одинаковых = 100, пара = 20, иначе = 0).

Errors

400 Bad Request — неверный формат JSON или структура запроса:
```
{ "error": "bad request" }
```
Примеры вызовов
```curl
curl -s -X POST http://127.0.0.1:5000/api/spin \
  -H "Content-Type: application/json" \
  -d '{"nickname":"Player1"}'
```

---

## 👥 Состав команды

- Баранов Александр (GitHub: Blond-beard-catboy) — Backend (Flask + API)  
- Белых Андрей (GitHub: Belandiy) — Документация + DevOps + БД
- Савчук Виталий (GitHub: vitek4k) — Верстальщик / UI-дизайнер
- Яшков Андрей (GitHub: Lomikk) — Фронтенд (JS)
- Джураев Фазлиддин (GitHub: Fazliddin4999) — Игровая логика

---

## 📌 Функционал

- Рулетка с 3 цилиндрами и анимацией вращения.  
- Кнопка **«Крутить»** для запуска игры.  
- Подсчёт очков за комбинации.  
- Сохранение результатов в базе данных.  
- Отображение турнирной таблицы (ТОП-10 игроков).  
- Страница «Правила игры».  
- Минимальная регистрация игрока (никнейм).  

---

## 📌 Документация

- [Техническое задание](tech-task.md)

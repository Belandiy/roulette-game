from secrets import choice

# символы и веса выпадения
SYMBOL_WEIGHTS = {
    "🍒": 3, 
    "🍋": 3, 
    "⭐": 2, 
    "🔔": 1, 
    "7️⃣": 1, 
}

# подготовим «барабан» для простого выбора
DECK = [sym for sym, w in SYMBOL_WEIGHTS.items() for _ in range(w)]


def spin_reels(n=3):
    """
    Заглушка для генерации симвалов.
    Возврощает фиксированное значение
    return  ["🍒", "🍒", "⭐" ] # Фиксированная комбинация для теста
    """

def score(reels):
    """
    Заглушка для подчета очков.
    Возврощает фиксированное значение
    """
    return 10

# Тестовый блок
if __name__ == "__main__":
    print("=== ТЕСТИРОВАНИЕ SCORING.PY ===")
    
    # Тест спина
    print("1. Тест спина:")
    test_reels = spin_reels(3)
    print(f"   Результат: {test_reels}")
    print(f"   Очки: {score(test_reels)}")
